# Public client (browser)
# Required. Get these from your Supabase project settings → Project API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (used by API routes)
# Service role key must be kept secret; NEVER expose in client bundles.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# Optional: site origin used for absolute callback links (falls back to http://localhost:3000)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Storage config (optional overrides)
PREAUTH_BUCKET=preauth-uploads
SIGNED_UPLOAD_TTL_SECONDS=3600

# After copying to .env.local, run `npm run dev` again to pick up changes.
